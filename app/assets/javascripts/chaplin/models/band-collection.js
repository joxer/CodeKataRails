/*define([
  'chaplin',
    'models/base/collection',
    'models/band'
], function(Chaplin, Collection, Band) {
  'use strict';

    var Bands = Collection.extend({

	model: Band,

	url: function(){

	    return "/bands"

	},
	defaults: {
	    
	}

    // ,initialize: function(attributes, options) {
    //  Model.prototype.initialize.apply(this, arguments);
    //  console.debug('HelloWorld#initialize');
    // }
  });

  return Bands;
});
*/
